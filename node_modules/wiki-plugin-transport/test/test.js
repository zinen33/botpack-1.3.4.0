"use strict";

(function () {
  // build time tests for transport plugin
  // see http://mochajs.org/
  var expect, transport;
  transport = require('../client/transport');
  expect = require('expect.js');
  describe('transport plugin', function () {
    return describe('expand', function () {
      return it('can make itallic', function () {
        var result;
        result = transport.expand('hello *world*');
        return expect(result).to.be('hello <i>world</i>');
      });
    });
  });
}).call(void 0);
//# sourceMappingURL=test.js.map
