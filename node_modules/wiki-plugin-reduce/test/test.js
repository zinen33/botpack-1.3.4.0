"use strict";

(function () {
  var expect, report;
  report = require('../client/reduce');
  expect = require('expect.js');
  describe('reduce plugin', function () {
    return describe('parsing', function () {
      it('recognizes FOLD', function () {
        return expect(report.parse("FOLD use these pages")).to.eql({
          find: "use these pages"
        });
      });
      it('recognizes WATCH', function () {
        return expect(report.parse("WATCH Bottles of Beer")).to.eql({
          watch: "Bottles of Beer"
        });
      });
      it('recognizes SLIDE', function () {
        return expect(report.parse("SLIDE Take Some Down")).to.eql({
          slide: "Take Some Down"
        });
      });
      return it('reports errors', function () {
        return expect(report.parse("watch the olympics")).to.eql({
          error: {
            line: 'watch the olympics',
            message: 'can\'t make sense of line'
          }
        });
      });
    });
  });
}).call(void 0);
//# sourceMappingURL=test.js.map
